@model IEnumerable<DohunKim.Models.ApplicationType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeName)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypeName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypeName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypeName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
