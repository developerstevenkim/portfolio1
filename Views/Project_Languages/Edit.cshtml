@model Do4.Models.Project_Language

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Project_Language</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
            </div>

            <div class="form-group">
                <label asp-for="ProjectName" class="control-label"></label>
                <select asp-for="ProjectName" class="form-control" asp-items="@ViewBag.ListOfProjects">
                </select>
            </div>
            <div class="form-group">
                <label asp-for="LanguageName" class="control-label"></label>
                <select asp-for="LanguageName" class="form-control" asp-items="@ViewBag.ListOfLanguages">
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
